const express = require('express');
const router = express.Router();

// Nog geen admin-routes geïmplementeerd — placeholder zodat server.js kan laden.
// Vul hier later de echte admin-endpoints in.

module.exports = router;